# POE Alarm 1.0.0

这是 POE Alarm 的第一个正式版本，核心目标是快速、清楚、可靠地判断屏幕上的完整词缀。

## 核心功能

- 保留成熟的单词缀快速模式，并兼容 0.6.1 的目标、选区、语言和快捷键设置。
- 新增多词缀组合：可以保存多种“值得停手的结果”，任意一种成立就报警。
- 每种结果支持“任意一条”“全部命中”或“至少命中 N 条”。
- 数值可选择忽略、至少、至多、精确值或闭区间；程序比较装备提示框中实际显示的值。
- 同一条实际词缀在一个结果中最多计数一次，避免相近文字或多行显示重复凑数。
- POE1 / POE2、English / 繁體中文分别保存配置，并共用同一套严格完整词缀规则。

## 稳定性修复

- 修复繁中小数可能被 OCR 显示为点状分隔符，导致 `3.73% 暴擊率` 的数值被错误拆开、无法命中的问题。
- 修复截图识别、停止监控与关闭程序同时发生时可能卡住或延迟弹出告警的问题。
- 设置保存改为串行处理，避免快速切换选项或关闭窗口时互相覆盖。
- 保留 0.6.1 已验证的快速监控、短暂输入保护和红色命中锁定行为。

## 本版主动移除

- 移除尚未通过实机安全验证的“谨慎模式 / Mirror Tier 安全模式”，包括黄色暂停、逐次点击放行和对应状态机。
- 旧预览配置若曾选择谨慎模式，升级后会自动使用快速模式；多词缀规则不会丢失。
- 该功能只有在交互逻辑闭环、真实高速制作回放充分，并能证明不会卡死或漏拦截后才会重新考虑。

## 运行环境

- Windows 10/11 x64。
- 发布包自带 .NET 与离线兼容识别引擎，无需安装 Python 或联网。
- 繁中建议安装 Windows `zh-TW` OCR，以获得更低延迟和更高稳定性。
- 当前公开包尚未进行代码签名，Windows 第一次运行时可能显示安全提醒。

## 安全边界

程序只读取屏幕像素，不会替玩家点击，也不会补发、重放或排队输入。正式使用前，请先用实际游戏画面和自己的选区做一轮截图测试与低速制作测试。
