# POE Alarm 0.4.4

这是繁體中文识别架构修复版。English 路径保持独立并完成基线回归。

## 修复

- 繁中默认改用 Windows 中文多行 OCR，不再把宽选区中被仓库蓝色图标粘连的多条词缀送入单行 Paddle 模型。
- 修复非 Alt 紧凑提示框中，数条词缀被合并后只识别出一个汉字、判定拖到数百毫秒的问题。
- Windows OCR 未严格命中时，只复核少量已定位的原色候选；内置 Paddle 改为局部验证和无中文语言能力时的兼容路径。
- 修复数字区间短横被中文 OCR 识别为“一”导致严格匹配漏报；仅在两个阿拉伯数字之间执行该修正。
- 保留繁中 changed-frame 鼠标闸门：疑难复核期间的后续点击被直接丢弃，不会点过目标帧。

## English 安全边界

- English 仍由原有 `WindowsOcrRecognizer` 处理，不使用繁中多行 OCR、汉字修正、Paddle 复核或未决鼠标闸门。
- 英文目标行 20/20、完整装备区 10/10；warm 延迟分别为 2.9–3.1 ms 与 26.2–28.1 ms，与旧基线一致。

## 回归验证

- 核心 matcher：29/29。
- 8.11 原生截图：6 张、27/27 命中、93 个跨截图负例误报 0。
- 旧微信/Alt 截图：65/65 命中。
- PoEDB 抽样：珠宝、深渊珠宝、武器、护甲共 163 条，合成目标语义命中 163/163（主结果 130、目标辅助 33、缓存 0）；712 个纯干扰、缺行、远距断层与有向近邻 OCR 负例误报 0。
- 13,203 组 PoEDB 模板两两检查：153 个不同模板之间误命中 0；完整 target-aware OCR 的 472 个有向人工近邻误报 0。
- 本机多次 Release 原生大选区：预检 p50 3.0–3.1 ms、目标判定 p50 27.0–29.2 ms；首次懒加载兼容模型的最坏单次判定 181–229.2 ms，处于鼠标保护期内。

## 环境说明

繁中快速路径优先使用 Windows `zh-TW`/`zh-Hant` OCR，其次尝试 `zh-Hans`/`zh-CN`。没有 Windows 中文 OCR 时程序仍可离线运行，但会使用较慢的内置兼容引擎。无需 Python、Paddle 框架或联网。
