# POE Alarm 0.4.2

本版修复繁中模式在 English 同级高速连点下可能完全错过短暂目标帧的问题。

## 主要变化

- 繁中 OCR 改为有界渐进扫描：每次最多处理两行 primary，或一个候选的一次分段复核，随后立即重新截屏。
- 已完成的物理行按内容指纹缓存；换行词缀只会在连续、已完成的行之间组合，不会跨过未知行误拼。
- CTC 同形字候选会优先尝试转换为严格分段命中；无法严格命中时，仍保留“全帧唯一候选”保护。
- 繁中画面变化后启用短暂的不可见输入盾。额外点击会被丢弃，不排队、不补发；未命中立即放行，命中则原地升级为居中红色确认窗。
- 输入盾采用原生按键配对状态机：先放行当前点击的完整 `MouseUp`，再从下一次 `MouseDown` 接管；被吞的 Down/Up 始终成对。OCR 未决期间红色 WPF 窗口保持隐藏，避免红框闪烁及装备 hover 被打断；真实命中后才显示并接管阻挡。
- 看门狗、手动停止、异常、关闭程序和 `Ctrl + Shift + F12` 都会优先恢复点击穿透。

## 验证

- 繁中真实截图：65/65 目标命中。
- 同 tooltip 交叉反例：594 次，误报 0。
- 正式 recognizer 端到端失败：0。
- 渐进 OCR 单时间片：p50 17.6–22.7 ms，p95 30.8–38.1 ms，最大 43.3 ms。
- matcher：29/29。
- English 真实截图端到端：通过。
- 40 ms 物理连点回放：普通、双行、CTC 辅助和候选第 3 行合计 35/35 命中，点过头 0、漏报 0、误报 0。
- 预热输入盾：最终测试首次呈现约 0.8–0.9 ms，热态约 0.3–0.5 ms；按键状态机、钩子安装/卸载、窗口样式、复用、停止/命中竞态断言通过。

## 行为说明

繁中完整 OCR 的最坏原始吞吐仍可能慢于 English。0.4.2 的处理方式不是把快速物理点击排队后继续执行，而是在每个变化帧完成可靠判定前丢弃后续点击。因此可以保持较快的手部点击节奏，但游戏内实际生效的制作次数会按 OCR 能安全处理的速度节流。
