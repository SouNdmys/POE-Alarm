# POE Alarm 0.6.0

## POE2 支持

- 主界面只需选择 `Path of Exile 1` 或 `Path of Exile 2`；两套目标词缀、框选区域和游戏语言分别保存。
- POE2 English 使用独立的双 Windows OCR 复核与局部识别模型验证；POE1 English 仍走独立的单 Windows OCR 配置，并已回归验证保持 0.4.0 同档。
- POE2 繁體中文优先使用 Windows `zh-TW` / `zh-Hant` OCR；缺少系统语言能力时仍可使用内置离线兼容引擎。
- POE2 统一支持最多 8 条相邻物理行，覆盖长碑牌组合词缀，无需额外切换“碑牌模式”。

## 匹配规则

- POE1 与 POE2 都会忽略词缀中的数值大小，只匹配完整文字、顺序和跨行结构；百分比/普通数及正/负仍作为防误报结构保留。
- 目标只在 2/3、词缀等级或 roll 高低上不同时会被视为同一条词缀；本工具不用于追踪精确数值。
- Attack/Cast、伤害类型、武器类型和条件句等文字差异仍必须完整一致。

## 验证摘要

- POE2 真实截图：English `38/38`、繁中 `38/38`，各 160 个跨截图目标负例误报 0。
- POE2 PoEDB 中英语料：167 组，其中碑牌 48 组；334/334 正例、562/562 缺行负例、298/298 范围实值、2/2 八行换行通过。
- POE2 繁中高速回放：30/40/50 ms 三档共 750 次，及时阻拦 750/750，点过头、截到却漏报和提前误报均为 0。
- POE1 English 基线复核：单行与完整装备区各 20/20，5 条真实词缀正例与 5 条语义近邻负例全部正确，延迟保持 0.4.0 同档。

以上结果来自当前测试机与已提供截图；首次在不同分辨率或 UI 缩放下使用时，仍建议先用“用截图测试”验证目标。
