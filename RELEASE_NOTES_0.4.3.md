# POE Alarm 0.4.3

这是 0.4.2 的输入保护热修复版。

## 修复

- 修复繁中模式开始监控后，全屏红色边框随 changed-frame 保护反复闪烁的问题。
- OCR 未决期间不再显示或命中测试全屏 WPF 窗口，只使用已经预装并同步武装的低级鼠标闸门。
- 避免透明全屏窗口抢走 POE 的装备 hover，造成提示框消失/重现、画面指纹变化以及保护循环自激。
- 红色外框与中央确认卡现在属于同一视觉层；可复用窗口隐藏时清空旧红色表面，只有真实命中才恢复显示。
- 原生钩子安装或重新武装失败时会拒绝开始或停止监控，不会显示“保护中”却实际无保护。
- 命中到红窗接管增加独立的 750 ms fail-open；即使 UI dispatcher 卡住，也不会无限锁住系统鼠标。

## 保持不变

- 繁中目标未决期间的后续按钮与滚轮输入仍会直接丢弃，不排队、不补发、不重放。
- 真正命中后仍显示居中的红色确认窗，并由该窗口接管鼠标阻挡，直到用户手动确认。
- English OCR 路径不安装 changed-frame 鼠标闸门，行为不变。

## 回归验证

- Release 全量解决方案构建：0 warning / 0 error。
- 29/29 matcher 测试通过。
- 连续 5 次 pending guard 刷新均验证 WPF overlay 与原生 HWND 保持隐藏。
- worker-thread 刷新、3 轮 Begin/Release，以及 UI dispatcher 阻塞时的 handoff 超时断言通过。
- 真正命中后的可复用红色阻挡窗生命周期与低级鼠标 hook 生命周期断言通过。
